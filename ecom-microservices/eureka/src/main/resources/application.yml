spring:
 application:
   name: eureka
server:
  port: 8761
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
logging:
  file:
    name: ${LOG_EUREKA}

  level:
    root: INFO
