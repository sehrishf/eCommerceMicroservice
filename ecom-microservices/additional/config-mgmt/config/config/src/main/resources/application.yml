spring:
 application:
   name: configdemo
 config:
   import: optional:configserver:http://localhost:8888

 profiles:
   active: dev

management:
  endpoints:
    web:
      exposure:
        include: refresh

#build:
#  id: ${ID}
#  version: ${VERSION}
#  name: ${NAME}

