spring:
  application:
    name: ecom-application

  datasource:
    url: jdbc:sqlserver://SVVWTDB10\\TESTNEU:1437;databaseName=ecomdb;encrypt=false;trustServerCertificate=true
    username: marzipan_user
    password: Elsoel4pl5!vf
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver


    #datasource:
    # url: jdbc:sqlserver://SVVWTDB10\\TESTNEU:1437;databaseName=ecomdb;encrypt=false;trustServerCertificate=true;integratedSecurity=true
  #  driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.SQLServerDialect

  flyway:
    enabled: true
    baseline-on-migrate: true

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always

